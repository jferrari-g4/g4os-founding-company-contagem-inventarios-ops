export default async function handler(req,res){const cnpj=String(req.query?.cnpj||'').replace(/\D/g,'');if(cnpj.length!==14)return res.status(400).json({error:'CNPJ deve ter 14 dígitos.'});try{const r=await fetch(`https://brasilapi.com.br/api/cnpj/v1/${cnpj}`);const body=await r.json();return res.status(r.status).json(body)}catch{return res.status(502).json({error:'Falha temporária na consulta do CNPJ.'})}}